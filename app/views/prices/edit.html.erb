<div class="container">
  <div class="row">
    <div class="col-xs-12 col-md-4 col-md-offset-4">
      <%= simple_form_for @price do |p| %>
        <%= p.input :value %>
        <%= p.input :currency %>
        <%= p.submit "Edit the prices", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
